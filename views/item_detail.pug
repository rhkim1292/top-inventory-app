extends layout

block content
  h1 #{itemName}

  p #[strong Price: ] #{itemPrice}
  p #[strong Quantity:] #{itemQuantity}
  p #[strong Category: ] #{itemCategory}

  hr
  p
    a(href=itemUrl+'/delete') Delete item
